# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A minimal web project currently containing only an empty `index.html` file. No build system, framework, or package manager has been configured yet.
